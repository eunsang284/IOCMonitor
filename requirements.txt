# IOC Monitor MCP Client Dependencies
# MCP 클라이언트 예시 실행을 위한 Python 의존성

# HTTP 요청을 위한 라이브러리
requests>=2.25.1

# 환경 변수 관리를 위한 라이브러리
python-dotenv>=0.19.0

# 데이터 처리를 위한 라이브러리 (선택사항)
pandas>=1.3.0
numpy>=1.21.0

# JSON 처리를 위한 라이브러리 (Python 3.6+에는 내장되어 있음)
# json

# 타입 힌트를 위한 라이브러리 (Python 3.7+에는 내장되어 있음)
# typing

# 데이터클래스를 위한 라이브러리 (Python 3.7+에는 내장되어 있음)
# dataclasses

# 날짜/시간 처리를 위한 라이브러리 (Python 3.6+에는 내장되어 있음)
# datetime

# 설치 방법:
# pip install -r requirements.txt
